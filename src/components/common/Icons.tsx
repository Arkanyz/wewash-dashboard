import { Droplet, CreditCard, Lock, Thermometer, Mic, LayoutDashboard, Building2, AlertTriangle, Wrench, BarChart2, Ticket, HelpCircle, MessagesSquare } from 'lucide-react';

// Export all icons as a single object
export const Icons = {
  Droplet,
  CreditCard,
  Lock,
  Thermometer,
  Mic,
  LayoutDashboard,
  Building2,
  AlertTriangle,
  Wrench,
  BarChart2,
  Ticket,
  HelpCircle,
  MessagesSquare,
} as const;
